import React from 'react'
import type { DiagramProps } from '../../types/diagram'

interface IProps extends DiagramProps {}

export const Canvas: React.FC<IProps> = () => {
  return (
    <div>asd</div>
  )
}
